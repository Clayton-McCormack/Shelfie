# The fake provider is the default and requires no credentials.
VISION_PROVIDER=fake

# Set VISION_PROVIDER=gemini and add a Google AI Studio key to enable real reads.
# GEMINI_API_KEY=replace-with-your-key
# GEMINI_MODEL=gemini-3.1-flash-lite

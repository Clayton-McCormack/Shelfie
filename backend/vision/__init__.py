"""Vision-provider boundary.

The API depends on this package rather than a specific hosted model. The fake
provider exercises the complete product flow without credentials or network
access; a hosted implementation can replace it without changing the endpoint.
"""

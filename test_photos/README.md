# Test photos

The bookshelf photos used to develop and measure the pipeline, committed so the
results in the main README can be reproduced.

These live here rather than in `backend/media/` because `media/` is gitignored —
it is the runtime upload destination and fills with uploads and generated spine
crops. This directory is the fixed, version-controlled input set.
